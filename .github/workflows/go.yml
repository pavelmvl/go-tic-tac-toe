name: Go

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:

  build:
    runs-on: golang-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Build
      run: go build -v ./...

    - name: Test
      run: go test -v ./...
